<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:label="@string/app_name"
        android:theme="@style/Theme.SilverServersCompanion"
        android:usesCleartextTraffic="true"
        tools:targetApi="32">
        <activity
            android:name=".biometrics"
            android:exported="false"
            android:label="@string/title_activity_biometrics"
            android:theme="@style/Theme.SilverServersCompanion.NoActionBar">
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity
            android:name=".DashboardActivity"
            android:exported="false"></activity>
        <activity
            android:name=".OauthActivity"
            android:exported="false"></activity>
        <activity
            android:name=".PasswordActivity"
            android:exported="false"></activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Services -->
        <service android:name="com.silverservers.service.location.LocationService" />
        <service android:name="com.silverservers.service.location.GeofenceService" />
    </application>

</manifest>